import 'package:flutter/material.dart';

class AppColors {
  // 🎨 Warna utama
  static const Color primary = Color(0xFF0196D6);
  static const Color secondary = Color(0xFFFF6B35);

  // 🧱 Background
  static const Color background = Color(0xFFF2F3F5);

  static const Color greyText = Color(0xff9A8B8B);

  // 🖋️ Teks
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Color(0xFF9A8B8B);

  // 🔘 Icon
  static const Color iconColor = Color(0xFFC6BFC0);

  // ⚠️ Error
  static const Color error = Colors.red;

  // 🌫️ Shadow
  static Color shadow = Colors.black.withOpacity(0.15);
}
